{
  "name": "qualityops-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Praxisnahes QA-Engineering-Portfolio mit Playwright, TypeScript, API-Tests, SQL und CI.",
  "scripts": {
    "test": "playwright test",
    "test:ai": "playwright test --config=playwright.ai.config.ts",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "typescript": "7.0.2"
  }
}
